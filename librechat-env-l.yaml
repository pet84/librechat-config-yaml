version: 1.2.4
cache: true

interface:
  customWelcome: "Vítej, {{user.name}} 👋 Jsem Airis ⚡"
  termsOfService:
    modalContent: |
      # Podmínky služby
      ...
  privacyPolicy:
    modalContent: |
      # Zásady ochrany osobních údajů
      ...

registration:
  socialLogins:
    - "google"

endpoints:
  custom:
    - name: "302AI"
      apiKey: "${AI302_API_KEY}"
      baseURL: "https://api.302.ai/v1"
      models:
        "gpt-4o":
          modelDisplayLabel: "⚡ 302 · GPT-4o"
        "gpt-4o-mini":
          modelDisplayLabel: "⚡ 302 · GPT-4o Mini"
        "claude-3-5-sonnet-20241022":
          modelDisplayLabel: "🎭 302 · Claude 3.5 Sonnet"
        "claude-3-5-haiku-20241022":
          modelDisplayLabel: "🎭 302 · Claude 3.5 Haiku"
        "gemini-2.5-flash":
          modelDisplayLabel: "🌐 302 · Gemini 2.5 Flash"
      fetch: false
      titleConvo: true
      titleModel: "gpt-4o-mini"
      summarize: false
      summaryModel: "gpt-4o-mini"
      modelDisplayLabel: "302AI"

speech:
  speechTab:
    conversationMode: false
    advancedMode: false
    speechToText:
      engineSTT: "browser"
      languageSTT: "Czech (CZ)"
      autoTranscribeAudio: false
      decibelValue: -45
      autoSendText: 1
    textToSpeech:
      engineTTS: "external"
      voice: "nova"
      languageTTS: "cs"
      automaticPlayback: true
      playbackRate: 1.0
      cacheTTS: true
  stt:
    openai:
      apiKey: "${OPENAI_API_KEY}"
      model: "whisper-1"
  tts:
    openai:
      apiKey: "${OPENAI_API_KEY}"
      model: "tts-1"
      voices: ["nova", "alloy", "echo", "fable", "onyx", "shimmer"]
