version: 1.2.8

endpoints:
  custom:
    - name: "OpenAI TEST" # <-- ZMĚNA ZDE
      apiKey: "${OPENAI_API_KEY}"
      baseURL: "https://api.openai.com/v1"
      models:
        "gpt-4o-mini":
          modelDisplayLabel: "Testovací Model Mini" # <-- A ZMĚNA ZDE
      fetch: false
      titleConvo: true
      titleModel: "gpt-4o-mini"
