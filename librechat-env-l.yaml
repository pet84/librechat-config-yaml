version: 1.2.8
cache: true

interface:
  customWelcome: "🌟 Vítej, {{user.name}} 👋 Jsem Airis"
  modelSpecs: true
  termsOfService:
    modalContent: |
      # Podmínky služby
      ## Úvod
      Vítejte v AIRIS ArchEnergy Chat!  
      Používáním této služby souhlasíte s následujícími pravidly:
      - Služba je poskytována „tak jak je".
      - AIRIS ArchEnergy nenese odpovědnost za rozhodnutí učiněná na základě výstupů AI.
      - Uživatel souhlasí, že AI může dělat chyby.
      ## Závěr
      Pokud nesouhlasíte s těmito podmínkami, službu nepoužívejte.
  privacyPolicy:
    modalContent: |
      # Zásady ochrany osobních údajů
      ## Ochrana dat
      Vaše soukromí je pro nás důležité.  
      Data konverzací nejsou sdílena s třetími stranami mimo poskytovatele AI API.
      - Osobní údaje uchováváme jen v nezbytném rozsahu.
      - Uživatel může kdykoliv požádat o smazání účtu.
      ## Kontakt
      V případě dotazů nás kontaktujte na [archenergy.cz](https://archenergy.cz).

registration:
  socialLogins:
    - "google"

endpoints:
  custom:
    - name: "OpenRouter"
      apiKey: "${OPENROUTER_KEY}"
      baseURL: "https://openrouter.ai/api/v1"
      fetch: true
      titleConvo: true
      titleModel: "openai/gpt-3.5-turbo"
      summarize: false
      summaryModel: "openai/gpt-3.5-turbo"
      forcePrompt: false
      modelDisplayLabel: "OpenRouter"

    - name: "Ollama"
      apiKey: "ollama"
      baseURL: "http://host.docker.internal:11434/v1/"
      fetch: true
      titleConvo: true
      titleModel: "llama3:latest"
      modelDisplayLabel: "Ollama"

speech:
  speechTab:
    conversationMode: false         # výchozí: vypnuto
    advancedMode: false
    speechToText:
      engineSTT: "browser"          # rychlé pro češtinu
      languageSTT: "Czech (CZ)"
      autoTranscribeAudio: false    # vypnuto ve výchozím stavu
      decibelValue: -45
      autoSendText: 1               # automatické odeslání po krátké pauze
    textToSpeech:
      engineTTS: "external"
      voice: "nova"                 # výchozí hlas Nova
      languageTTS: "cs"
      automaticPlayback: true
      playbackRate: 1.0
      cacheTTS: true
  stt:
    openai:
      apiKey: "${OPENAI_API_KEY}"
      model: "whisper-1"
  tts:
    openai:
      apiKey: "${OPENAI_API_KEY}"
      model: "tts-1"
      voices: ["nova", "alloy", "echo", "fable", "onyx", "shimmer"]

modelSpecs:
  enforce: false
  prioritize: true
  list:
    # OpenRouter
    - name: "or-3.5"
      label: "🟢 OpenRouter · GPT-3.5 Turbo"
      default: true
      preset:
        endpoint: "OpenRouter"
        model: "openai/gpt-3.5-turbo"
        modelLabel: "GPT-3.5 Turbo"

    - name: "or-4o-mini"
      label: "⚡ OpenRouter · GPT-4o Mini"
      preset:
        endpoint: "OpenRouter"
        model: "openai/gpt-4o-mini"
        modelLabel: "GPT-4o Mini"

    - name: "or-4o"
      label: "⚡ OpenRouter · GPT-4o"
      preset:
        endpoint: "OpenRouter"
        model: "openai/gpt-4o"
        modelLabel: "GPT-4o"

    # Ollama
    - name: "ol-llama3"
      label: "🦙 Ollama · Llama 3 (latest)"
      preset:
        endpoint: "Ollama"
        model: "llama3:latest"
        modelLabel: "Llama 3"

    - name: "ol-mixtral"
      label: "🌀 Ollama · Mixtral 8x7B"
      preset:
        endpoint: "Ollama"
        model: "mixtral:8x7b-instruct-v0.1"
        modelLabel: "Mixtral 8x7B"

    - name: "ol-phi3"
      label: "🔹 Ollama · Phi-3 Mini 4k"
      preset:
        endpoint: "Ollama"
        model: "phi3:mini-4k-instruct"
        modelLabel: "Phi-3 Mini 4k"
