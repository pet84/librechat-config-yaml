version: 1.2.8
cache: true

interface:
  customWelcome: "🌟 Vítej, {{user.name}} 👋 Jsem Airis"
  customFooter: "© 2025 AIRIS ArchEnergy · AI může dělat chyby"
  hideModelsFromUI: false
  promptTemplates: true
  modelSpecs: true
  termsOfService:
    modalContent: |
      # Podmínky služby
      ## Úvod
      Vítejte v AIRIS ArchEnergy Chat! Používáním této služby souhlasíte s následujícími pravidly:
      - Služba je poskytována „tak jak je".
      - AIRIS ArchEnergy nenese odpovědnost za rozhodnutí učiněná na základě výstupů AI.
      - Uživatel souhlasí, že AI může dělat chyby.
      ## Závěr
      Pokud nesouhlasíte s těmito podmínkami, službu nepoužívejte.
  privacyPolicy:
    modalContent: |
      # Zásady ochrany osobních údajů
      ## Ochrana dat
      Vaše soukromí je pro nás důležité. Data konverzací nejsou sdílena s třetími stranami mimo poskytovatele AI API.
      - Osobní údaje uchováváme jen v nezbytném rozsahu.
      - Uživatel může kdykoliv požádat o smazání účtu.
      ## Kontakt
      V případě dotazů nás kontaktujte na [archenergy.cz](https://archenergy.cz).

registration:
  socialLogins: ["google"]

endpoints:
  # ---------------------------------------------------------------------------
  # DŮLEŽITÉ: Asistenti jsou speciální endpoint a zůstávají mimo 'custom' sekci
  # ---------------------------------------------------------------------------
  assistants:
    supportedIds:
      - "asst_MLMO60C5sEqtNLFbUVAc6fy6"
    titleConvo: true
    titleModel: "gpt-4o-mini"
    modelDisplayLabel: "⚡ Airis"

  # ---------------------------------------------------------------------------
  # Zde definujeme VŠECHNY ostatní endpointy jako 'custom' seznam
  # ---------------------------------------------------------------------------
  custom:
    - name: "OpenAI" # Toto jméno se zobrazí v UI
      apiKey: "${OPENAI_API_KEY}"
      baseURL: "https://api.openai.com/v1"
      titleConvo: true
      titleModel: "gpt-4o-mini"
      summarize: false
      models:
        "gpt-5-chat-latest":
          modelDisplayLabel: "GPT-5 (Nejnovější)"
        "gpt-4o":
          modelDisplayLabel: "GPT-4o"
        "gpt-4o-mini":
          modelDisplayLabel: "GPT-4o Mini"
      fetch: false

    - name: "Google" # Toto jméno se zobrazí v UI
      apiKey: "${GOOGLE_KEY}" # Použije vaši proměnnou GOOGLE_KEY
      # LibreChat má pro Google speciální baseURL, toto je pro OpenAI-kompatibilní endpointy
      # Pokud by to nefungovalo, může být potřeba speciální proxy
      baseURL: "https://generativelanguage.googleapis.com/v1beta" 
      titleConvo: true
      titleModel: "gemini-1.5-flash"
      summarize: false
      models:
        "models/gemini-1.5-pro-latest":
          modelDisplayLabel: "Gemini 1.5 Pro"
        "models/gemini-1.5-flash-latest":
          modelDisplayLabel: "Gemini 1.5 Flash"
      # Google API v custom režimu může vyžadovat specifické parametry
      # Můžeme je přidat později, pokud bude potřeba

    - name: "Anthropic" # Toto jméno se zobrazí v UI
      apiKey: "${ANTHROPIC_API_KEY}"
      baseURL: "https://api.anthropic.com/v1"
      # Anthropic vyžaduje speciální hlavičky, které musíme přidat
      headers:
        "x-api-key": "${ANTHROPIC_API_KEY}"
        "anthropic-version": "2023-06-01"
      titleConvo: true
      titleModel: "claude-3-haiku-20240307"
      summarize: false
      models:
        "claude-3-5-sonnet-20240620":
          modelDisplayLabel: "Claude 3.5 Sonnet"
        "claude-3-opus-20240229":
          modelDisplayLabel: "Claude 3 Opus"
      # Anthropic také vyžaduje specifické parametry, toto je základ

# -----------------------------------------------------------------------------
# Ostatní nastavení zůstávají stejná
# -----------------------------------------------------------------------------
speech:
  speechTab:
    conversationMode: true
    advancedMode: false
    speechToText:
      engineSTT: "browser"
      languageSTT: "Czech (CZ)"
      autoTranscribeAudio: true
      decibelValue: -45
      autoSendText: 3
    textToSpeech:
      engineTTS: "external"
      voice: "nova"
      languageTTS: "cs"
      automaticPlayback: true
      playbackRate: 1.0
      cacheTTS: true
  stt:
    openai:
      apiKey: "${STT_OPENAI_API_KEY}"
      model: "whisper-1"
  tts:
    openai:
      apiKey: "${TTS_OPENAI_API_KEY}"
      model: "tts-1"
      voices: ["nova", "alloy", "echo", "fable", "onyx", "shimmer"]

rateLimits:
  fileUploads:
    ipMax: 100
    ipWindowInMinutes: 60
    userMax: 50
    userWindowInMinutes: 60

logging:
  level: "info"
  hideIPs: true
