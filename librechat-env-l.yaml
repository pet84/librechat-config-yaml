# -----------------------------------------------------------------------------
# LibreChat Konfigurace pro Airis - Verze 1.2.8 (Září 2025)
# Tento soubor je syntakticky opravený a připravený k použití.
# -----------------------------------------------------------------------------
version: 1.2.8
cache: true

# -----------------------------------------------------------------------------
# Uživatelské rozhraní
# -----------------------------------------------------------------------------
interface:
  customWelcome: "🌟 Vítej, {{user.name}} 👋 Jsem Airis"
  customFooter: "© 2025 AIRIS ArchEnergy · AI může dělat chyby"
  hideModelsFromUI: false
  promptTemplates: true
  modelSpecs: true
  termsOfService:
    modalContent: |
      # Podmínky služby
      
      ## Úvod
      Vítejte v AIRIS ArchEnergy Chat! Používáním této služby souhlasíte s následujícími pravidly:
      - Služba je poskytována „tak jak je".
      - AIRIS ArchEnergy nenese odpovědnost za rozhodnutí učiněná na základě výstupů AI.
      - Uživatel souhlasí, že AI může dělat chyby.
      
      ## Závěr
      Pokud nesouhlasíte s těmito podmínkami, službu nepoužívejte.
  privacyPolicy:
    modalContent: |
      # Zásady ochrany osobních údajů
      
      ## Ochrana dat
      Vaše soukromí je pro nás důležité. Data konverzací nejsou sdílena s třetími stranami mimo poskytovatele AI API.
      - Osobní údaje uchováváme jen v nezbytném rozsahu.
      - Uživatel může kdykoliv požádat o smazání účtu.
      
      ## Kontakt
      V případě dotazů nás kontaktujte na [archenergy.cz](https://archenergy.cz).

# -----------------------------------------------------------------------------
# Registrace a přihlášení
# -----------------------------------------------------------------------------
registration:
  socialLogins: ["google"]

# -----------------------------------------------------------------------------
# Konfigurace AI modelů (Endpointy)
# -----------------------------------------------------------------------------
endpoints:
  assistants:
    # Povolí pouze jednoho konkrétního asistenta a přejmenuje záložku
    supportedIds:
      - "asst_MLMO60C5sEqtNLFbUVAc6fy6"
    titleConvo: true
    titleModel: "gpt-4o-mini" # Používá rychlý model pro generování názvů konverzací
    modelDisplayLabel: "⚡ Airis"

  openAI:
    titleConvo: true
    titleModel: "gpt-4o-mini"
    summarize: false
    models:
      "gpt-5-chat-latest":
        modelDisplayLabel: "GPT-5 (Nejnovější)"
      "gpt-5-2025-08-07":
        modelDisplayLabel: "GPT-5 (08-07)"
      "gpt-4o":
        modelDisplayLabel: "GPT-4o"
    fetch: false

  google:
    titleConvo: true
    titleModel: "gemini-1.5-flash" # Pro titulky je lepší použít rychlejší a levnější model
    summarize: false
    models:
      "gemini-2.5-pro":
        modelDisplayLabel: "Gemini 2.5 Pro"
      "gemini-2.5-flash":
        modelDisplayLabel: "Gemini 2.5 Flash"
    fetch: false

  anthropic:
    apiKey: "${ANTHROPIC_API_KEY}"
    titleConvo: true
    titleModel: "claude-3-haiku-20240307" # Ověřte si nejnovější rychlý model
    summarize: false
    models:
      # DŮLEŽITÉ: Názvy nových Claude modelů si ověřte v oficiální dokumentaci.
      # Níže jsou příklady, jak by mohly vypadat. Nahraďte je skutečnými ID.
      "claude-4-sonnet-2025-08-15": # Příklad - nahraďte skutečným ID
        modelDisplayLabel: "Claude 4 Sonnet"
      "claude-3-5-sonnet-20240620":
        modelDisplayLabel: "Claude 3.5 Sonnet"
    fetch: false

# -----------------------------------------------------------------------------
# Hlasové funkce (Speech-to-Text a Text-to-Speech)
# -----------------------------------------------------------------------------
speech:
  speechTab:
    conversationMode: true
    advancedMode: false
    speechToText:
      engineSTT: "browser"
      languageSTT: "Czech (CZ)"
      autoTranscribeAudio: true
      decibelValue: -45
      autoSendText: 3
    textToSpeech:
      engineTTS: "external"
      voice: "nova"
      languageTTS: "cs"
      automaticPlayback: true
      playbackRate: 1.0
      cacheTTS: true
  stt:
    openai:
      apiKey: "${STT_OPENAI_API_KEY}"
      model: "whisper-1"
  tts:
    openai:
      apiKey: "${TTS_OPENAI_API_KEY}"
      model: "tts-1"
      voices: ["nova", "alloy", "echo", "fable", "onyx", "shimmer"]

# -----------------------------------------------------------------------------
# Ochrana proti spamu (Rate Limiting)
# -----------------------------------------------------------------------------
rateLimits:
  fileUploads:
    ipMax: 100
    ipWindowInMinutes: 60
    userMax: 50
    userWindowInMinutes: 60

# -----------------------------------------------------------------------------
# Logování
# -----------------------------------------------------------------------------
logging:
  level: "info"
  hideIPs: true
