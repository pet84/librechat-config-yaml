version: 1.2.8
cache: true

interface:
  customWelcome: "🌟 Vítej, {{user.name}} 👋 Jsem Airis"
  termsOfService:
    modalContent: |
      # Podmínky služby
      ## Úvod
      Vítejte v AIRIS ArchEnergy Chat!  
      Používáním této služby souhlasíte s následujícími pravidly:
      - Služba je poskytována „tak jak je".
      - AIRIS ArchEnergy nenese odpovědnost za rozhodnutí učiněná na základě výstupů AI.
      - Uživatel souhlasí, že AI může dělat chyby.
      ## Závěr
      Pokud nesouhlasíte s těmito podmínkami, službu nepoužívejte.
  privacyPolicy:
    modalContent: |
      # Zásady ochrany osobních údajů
      ## Ochrana dat
      Vaše soukromí je pro nás důležité.  
      Data konverzací nejsou sdílena s třetími stranami mimo poskytovatele AI API.
      - Osobní údaje uchováváme jen v nezbytném rozsahu.
      - Uživatel může kdykoliv požádat o smazání účtu.
      ## Kontakt
      V případě dotazů nás kontaktujte na [archenergy.cz](https://archenergy.cz).

registration:
  socialLogins: ["google"]

endpoints:
  custom:
    # OpenAI
    - name: "OpenAI"
      apiKey: "${OPENAI_API_KEY}"
      baseURL: "https://api.openai.com/v1"
      titleConvo: true
      titleModel: "gpt-4o-mini"   # výchozí model
      fetch: false
      models:
        "gpt-4o":
          modelDisplayLabel: "ChatGPT 4o"
        "gpt-4o-mini":
          modelDisplayLabel: "ChatGPT 4o Mini"

    # Google Gemini (přímé API)
    - name: "Google"
      apiKey: "${GOOGLE_KEY}"
      baseURL: "https://generativelanguage.googleapis.com/v1beta"
      titleConvo: true
      titleModel: "models/gemini-2.5-flash"
      fetch: false
      models:
        "models/gemini-2.5-pro":
          modelDisplayLabel: "Gemini 2.5 Pro"
        "models/gemini-2.5-flash":
          modelDisplayLabel: "Gemini 2.5 Flash"
        "models/gemini-2.5-flash-lite":
          modelDisplayLabel: "Gemini 2.5 Flash Lite"

    # Anthropic
    - name: "Anthropic"
      apiKey: "${ANTHROPIC_API_KEY}"
      baseURL: "https://api.anthropic.com/v1"
      headers:
        "x-api-key": "${ANTHROPIC_API_KEY}"
        "anthropic-version": "2023-06-01"
      titleConvo: true
      titleModel: "claude-3-5-sonnet-20241022"
      fetch: false
      models:
        "claude-3-5-sonnet-20241022":
          modelDisplayLabel: "Claude 3.5 Sonnet"
        "claude-3-5-haiku-20241022":
          modelDisplayLabel: "Claude 3.5 Haiku"
        "claude-3-opus-20240229":
          modelDisplayLabel: "Claude Opus"

speech:
  speechTab:
    conversationMode: false
    advancedMode: false
    speechToText:
      engineSTT: "browser"
      languageSTT: "Czech (CZ)"
      autoTranscribeAudio: false
      decibelValue: -45
      autoSendText: 1
    textToSpeech:
      engineTTS: "external"
      voice: "nova"
      languageTTS: "cs"
      automaticPlayback: true
      playbackRate: 1.0
      cacheTTS: true
  stt:
    openai:
      apiKey: "${OPENAI_API_KEY}"
      model: "whisper-1"
  tts:
    openai:
      apiKey: "${OPENAI_API_KEY}"
      model: "tts-1"
      voices: ["nova", "alloy", "echo", "fable", "onyx", "shimmer"]
